import { tFilterDemande } from '../../types/demande.type';

const initFilterDemande: tFilterDemande = {
  profession: '',
  status: '',
  limit: 10,
  page: 1,
  searchTerm: '',
};

export default initFilterDemande;
